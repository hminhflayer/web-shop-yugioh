@model WebShop.Models.Provider

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>Provider</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.ProviderName)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.ProviderName)
        </dd>
    </dl>
</div>
<div>
    <div class="row">
        <div class="col-sm">
            <a asp-action="Index" class="no_underline"><i class="fas fa-arrow-left"></i> Về trang quản lý</a>
        </div>
        <div class="col-sm text-end">
            <a class="btn btn-success" asp-action="Edit" asp-route-id="@Model.ProviderID">Chỉnh sửa</a>
        </div>
    </div>
</div>